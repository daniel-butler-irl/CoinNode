apiVersion: v1
kind: Service
metadata:
  name: bitcoin-node-rpc
  labels:
    app: bitcoin-node
spec:
  type: ClusterIP
  selector:
    app: bitcoin-node
  ports:
    - name: rpc
      port: 18443
      targetPort: rpc
