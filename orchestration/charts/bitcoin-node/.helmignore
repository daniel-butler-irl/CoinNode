# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~
# Various IDEs
.project
.idea/
*.tmproj
.vscode/
# Helm
.helmignore
# CI/CD
.github/
.gitlab-ci.yml
.travis.yml
Jenkinsfile
# Documentation source (keep rendered docs)
docs/
# Test files
*_test.yaml
*.test.yaml
# Local overrides
values-local.yaml
values.local.yaml
